import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blogs",
  description: "Explore a collection of technical blogs and articles shared by developers in the DevCommunity platform.",
};

export default function BlogPage() {
  return (
    <h1>Blog Page</h1>
  )
}